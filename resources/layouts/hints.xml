<!-- Base StartHint layout: kept EMPTY so it compiles on every device.
     Devices that expose the 'system_..._hint_button_right_top' personality get
     the native hint arc via resources-hints/ (added to their resourcePath in
     monkey.jungle). DataView tolerates a missing 'startHint' drawable. -->
<layout id="StartHint">
</layout>
